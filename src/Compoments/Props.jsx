import React from 'react'

const Props = (props) => {
    return (
        <div>
            <div>
                {props.message}
            </div>
        </div>
    )
}

// Props.defaultProps = {
//     message: "Default Message",
// };

export default Props